<%- include('partials/head', { title, apiBase, active }) %>

<div class="flex items-start justify-between gap-4">
  <div>
    <h1 class="text-2xl font-semibold tracking-tight text-gray-900">Emails</h1>
    <p class="mt-1 max-w-2xl text-sm text-gray-600">Ingested .eml threads treated as first-class knowledge sources.</p>
  </div>
  <button id="add-email" class="shrink-0 rounded-lg bg-generated px-3 py-2 text-sm font-medium text-white hover:opacity-90">
    + Add email
  </button>
</div>

<div id="emails-list" class="mt-6 grid gap-3">
  <p class="text-sm text-gray-500">Loading…</p>
</div>

<div id="email-modal" class="hidden"></div>

<%- include('partials/foot', { clientScript }) %>
