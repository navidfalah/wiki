<%- include('partials/head', { title, apiBase, active }) %>

<h1 class="text-2xl font-semibold tracking-tight text-gray-900">Resources</h1>
<p class="mt-1 max-w-2xl text-sm text-gray-600">Every source cited across the wiki, deduped into one list.</p>

<input id="resources-search" type="search" placeholder="Search resources…" class="mt-4 w-full max-w-sm rounded-lg border border-gray-300 px-3 py-1.5 text-sm focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/20" />

<div id="resources-list" class="mt-4 grid gap-2">
  <p class="text-sm text-gray-500">Loading…</p>
</div>

<%- include('partials/foot', { clientScript }) %>
