<%- include('partials/head', { title, apiBase, active }) %>

<h1 class="text-2xl font-semibold tracking-tight text-gray-900">Connectors</h1>
<p class="mt-1 max-w-2xl text-sm text-gray-600">
  Connect Gmail, Google Drive, or any IMAP mailbox and pull content straight into
  <code class="rounded bg-gray-100 px-1 py-0.5">data/raw/connectors/</code> -- from there it flows through the
  normal compiler pipeline like anything else. Credentials are encrypted at rest and never
  leave this server. See <code class="rounded bg-gray-100 px-1 py-0.5">documentation/34-external-connectors.md</code>
  for the security model.
</p>

<div id="connectors-list" class="mt-6 flex flex-col gap-4"></div>

<%- include('partials/foot', { clientScript }) %>
