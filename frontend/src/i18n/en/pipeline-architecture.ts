export const pipelineArchitecture = {
  'pipeline-architecture.title': 'Pipeline Architecture',
  'pipeline-architecture.introHtml': 'Configure how the compiler processes raw sources into wiki pages. These are saved defaults -- every future run on the <a href="/pipelines" class="text-generated underline">Pipelines</a> page uses them automatically.',
  'pipeline-architecture.stages': 'Pipeline stages',
  'pipeline-architecture.stagesHint': '1. Read → 2. Extract → 3. Synthesize → 4. Index → 5. Cross-link. These toggles change stage 2 (extraction) and stage 3 (synthesis) behavior.',
  'pipeline-architecture.critic': 'Critic pass',
  'pipeline-architecture.criticHint': 'After synthesis, run a second LLM pass that strips sentences it can\'t ground in the source chunks. Roughly doubles per-page cost.',
  'pipeline-architecture.criticSamples': 'Critic samples (self-consistency)',
  'pipeline-architecture.criticSamplesHint': 'Run the critic this many times per page; only strip a sentence a majority flagged. 1 = single deterministic pass.',
  'pipeline-architecture.regenerate': 'Regenerate heavily-stripped pages',
  'pipeline-architecture.regenerateHint': 'When the critic strips more than 20% of a page, regenerate it once with the critic\'s findings fed back into the prompt.',
  'pipeline-architecture.corrections': 'Use active-learning corrections',
  'pipeline-architecture.correctionsHint': 'Feed prior human review corrections into the extraction prompt as few-shot examples.',
  'pipeline-architecture.pii': 'Redact PII before extraction',
  'pipeline-architecture.piiHint': 'Strip SSNs, credit cards, API keys, phone numbers, and IPv4 addresses from chunk text before it\'s sent to the LLM. Does not redact email addresses or names.',
  'pipeline-architecture.web': 'Web search enrichment',
  'pipeline-architecture.webHintHtml': 'During synthesis, search the internet for each topic being (re)generated and add the results as extra sources alongside <code class="rounded bg-white px-1 py-0.5">data/raw/</code>, tagged <code class="rounded bg-white px-1 py-0.5">source_type: web</code> and trusted "Low" by default. Requires network access from the compiler process.',
  'pipeline-architecture.sources': 'Active data sources',
  'pipeline-architecture.sourcesHintHtml': 'Uncheck a top-level <code>data/raw/</code> folder to leave it out of every build. Excluded files are skipped, not deleted -- their topics come back automatically once re-enabled, no rebuild needed.',
  'pipeline-architecture.noFolders': 'No folders found under data/raw/ yet.',
  'pipeline-architecture.saving': 'Saving…',
  'pipeline-architecture.savedAt': 'Saved at {time}',
  'pipeline-architecture.saved': 'Pipeline architecture saved.',
  'pipeline-architecture.saveFailed': 'Could not save pipeline architecture.',
} as const;
